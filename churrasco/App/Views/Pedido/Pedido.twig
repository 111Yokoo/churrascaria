<main style="margin-top: 150px;">
    <!--<label for="frutas">Escolha suas frutas favoritas:</label>
  <select id="frutas" name="frutas" multiple>
    <option value="maca">Maçã</option>
    <option value="banana">Banana</option>
    <option value="laranja">Laranja</option>
    <option value="uva">Uva</option>
    <option value="morango">Morango</option>
  </select>

  <script>
    // Adiciona um ouvinte de eventos ao elemento select
    document.getElementById('frutas').addEventListener('change', function() {
      // Obtém os elementos selecionados e os armazena para uso posterior
      let selectedValues = Array.from(this.selectedOptions).map(option => option.value);

      // Exibe os valores selecionados no console (você pode fazer o que quiser com eles aqui)
      console.log(selectedValues);
    });
  </script>-->
  <section class="container">
    <section class="row">
      <article class="col-8">
        <section class="row">
          <article class="col-4 carnesInput">Carnes</article>
          <article class="col-2 input">Adicionais</article>
          <article class="col-2 input">Saladas</article>
          <article class="col-2 input">Bebidas</article>
          <article class="col-2 input">Sobremesas</article>
        </section>
      </article>
      <article class="col-4">
        <h1>teste</h1>
      </article>
    </section>
  </section>
</main>
